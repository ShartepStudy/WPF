<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApplication4" x:Class="WpfApplication4.MainWindow"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <Window.Resources>
        <ObjectDataProvider x:Key="objectProvider" MethodName="GetXmlDocument"/>
        <XmlDataProvider x:Key="xmlProvider"/>
		<HierarchicalDataTemplate x:Key="XmlDataTemplate" ItemsSource="{Binding XPath=item}">
			<TextBlock Text="{Binding}"/>
		</HierarchicalDataTemplate>
    </Window.Resources>
    <Window.DataContext>
		<local:MainViewModel/>
	</Window.DataContext>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="8*"/>
			<RowDefinition Height="9*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<ListBox Grid.Row="1" ItemsSource="{Binding Numbers}" Margin="5" Grid.RowSpan="2"/>
		<Button Content="Command" Command="{Binding Command, Mode=OneWay}" Margin="5"/>
		<TextBlock Grid.Column="1" Grid.Row="2" TextWrapping="Wrap" Text="{Binding Mode=OneWay, Source={StaticResource objectProvider}}"/>
		<TreeView Grid.Column="1" Grid.RowSpan="2" ItemsSource="{Binding Source={StaticResource xmlProvider}, XPath=root}"/>
        
	</Grid>
</Window>
